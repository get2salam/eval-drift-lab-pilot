# Eval Drift Lab

Build a brand-new, public, recruiter-facing software/AI engineering mini-project.

Repo name: eval-drift-lab-pilot
Description: CLI toolkit that compares model-evaluation runs and flags metric drift with JSON fixtures.
Preferred stack: Python stdlib + unittest
Must have: JSON eval-run parser, drift summary CLI, fixture-based tests, README with example output
